# Directus (handles both CMS and auth — deploy Directus then fill these in)
NEXT_PUBLIC_DIRECTUS_URL=       # e.g. https://your-directus.com
DIRECTUS_TOKEN=                 # Static admin token for server-side reads
